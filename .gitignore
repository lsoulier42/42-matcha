# Credentials — exigence du sujet (ne JAMAIS committer)
.env
*.env
!.env.example

# PHP / Composer (pas de dépendances autorisées, mais au cas où)
/vendor/
composer.lock

# Docker
/docker-data/

# Cache & logs applicatifs
/var/

# Artefacts de session ZCode (plans, etc.)
/.zcode/

# Photos uploadées par les utilisateurs (jamais dans git)
/public/assets/uploads/*
!/public/assets/uploads/.htaccess

# Logs & temp
*.log
/tmp/

# OS / éditeurs
.DS_Store
.idea/
.vscode/
